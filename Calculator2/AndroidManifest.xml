<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tombarrasso.android.calculator2"
    android:versionCode="1"
    android:versionName="One">
    
    <uses-sdk
    	android:minSdkVersion="7"
    	android:targetSdkVersion="10"
    	android:maxSdkVersion="10" />
    	
    <!--
    	Modified Calculator2 to support Android 2.1 (API 7)
    	until Android 2.3 (API 10). There is no need to support
    	Android 3.x+ since this calculator has not changed
    	since then.
     -->

    <application
    	android:label="@string/app_name"
    	android:icon="@mipmap/ic_launcher_calculator">
        <activity android:name="com.android.calculator2.Calculator" 
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest> 
